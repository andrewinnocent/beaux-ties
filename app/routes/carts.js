import Route from '@ember/routing/route'

export default Route.extend({
  // model() needed? Points to table?
  // cart logic here?
    // cart starting as an empty object?
    // add qty, price, bow_id
  // actions: {
    // add/subtract quantity in Cart
    // remove beauxtie from Cart
    // checkout (with alert of purchase successful)
  // }
})
