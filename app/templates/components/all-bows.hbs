<div class="col-sm-6 col-md-4 col-lg-4">
  <div class="row">
    <div class="thumbnail">
      {{#link-to 'bow' bow}}<img src={{bow.image}} alt="{{bow.name}}">{{/link-to}}
      <div class="caption">
        {{#link-to 'bow' bow}}<h3>{{bow.name}}</h3>
        More Details{{/link-to}}
        <h4>{{bow.style}}</h4>
        <h3>${{bow.price}}</h3>
        {{#if isAuthenticated}}
        {{!-- <h4>Add to Wishlist {{wishlist-checkbox}}</h4> --}}
        {{wishlist-button}}
        {{add-to-cart bow=bow addToCart='addToCart'}}
        {{else}}
        {{#link-to 'sign-up'}} Sign Up {{/link-to}} or {{#link-to 'sign-in'}} Sign In to Shop {{/link-to}}
        {{/if}}
      </div>
    </div>
  </div>
</div>
